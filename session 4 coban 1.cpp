#include<stdio.h>

int main(){
	int number;
	printf("hay nhap mot so tu nhien ");
	scanf("%d",&number);
	if(number>=0){
		printf("so nhap vao la so duong");
		}
		else{
			printf("so nhap vao la so am");
			}
			return 0;
}
			
